{
  "name": "lingua-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Lingua — single-file language learning & translation PWA, with a zero-dependency Claude proxy for accurate AI answers (Node 18+ built-in fetch).",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.mjs"
  }
}
