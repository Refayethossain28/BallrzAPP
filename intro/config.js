/* Intro — optional integrations. Safe to leave untouched: with an empty
 * passEndpoint the app works fully offline and the 🎟 Wallet button explains
 * how to enable Apple Wallet passes (see WALLET.md). Set passEndpoint to your
 * deployed intro/server.mjs to serve real signed .pkpass files. */
window.IntroConfig = {
  passEndpoint: ''
};
