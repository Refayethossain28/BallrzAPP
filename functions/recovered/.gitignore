# Landing area for the deployed gen-1 function source pulled off the live project
# (see ../../docs/apexvip-backend-consolidation.md §3). Everything here is review
# scratch — never commit recovered source or its node_modules. Keep only this
# .gitignore and the README.
*
!.gitignore
!README.md
