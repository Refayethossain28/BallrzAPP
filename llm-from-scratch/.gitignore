*.npz
__pycache__/
*.pyc
py_ref.json

# Downloaded corpora (multi-MB) — never committed; regenerate on demand
# (fetch_data.py shakespeare, Norvig's big.txt, or WikiText-2 for wiki.txt).
data/shakespeare.txt
data/big.txt
data/wiki.txt
data/corpus.txt

# web/model.json is intentionally committed: it's the trained weights the
# deployed browser PWA (GitHub Pages) ships with. Regenerate with export_web.py.
# The standalone single-file build stays out of git — it's easy to rebuild.
web/llm.html
data/hub.txt
