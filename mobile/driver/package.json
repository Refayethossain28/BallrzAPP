{
  "name": "apexvip-ios-driver",
  "version": "1.0.0",
  "private": true,
  "description": "ApexVIP driver app — iOS wrapper (Capacitor).",
  "scripts": {
    "build": "node ../build-www.mjs driver",
    "sync": "npm run build && npx cap sync ios",
    "ios": "npm run sync && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
