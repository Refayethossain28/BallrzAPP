{
  "name": "apexchain",
  "description": "ApexCoin (AXC) on-chain — the ApexVIP loyalty coin as a standard ERC-20, plus its compile/test/deploy tooling.",
  "private": true,
  "type": "module",
  "scripts": {
    "compile": "node compile.mjs",
    "test": "node --test test/apexcoin.test.mjs",
    "deploy": "node deploy.mjs"
  },
  "devDependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "ethers": "^6.17.0",
    "ganache": "^7.9.2",
    "solc": "^0.8.28"
  }
}
