{
  "name": "atlas-ios",
  "version": "1.0.0",
  "private": true,
  "description": "Atlas — App Store wrapper. Bundles ../atlas into a Capacitor iOS app.",
  "scripts": {
    "bundle": "node bundle-www.mjs",
    "sync": "npm run bundle && npx cap sync ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  }
}
