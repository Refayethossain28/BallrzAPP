{
  "name": "apexvip-ios",
  "version": "1.0.0",
  "description": "ApexVIP iOS app (Capacitor wrapper around the client web app)",
  "private": true,
  "scripts": {
    "sync": "cp ../apexvip-client.html www/index.html && cp ../firebase.js ../apexvip-core.js www/ && npx cap copy ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0",
    "@capacitor/ios": "^8.4.0"
  }
}
